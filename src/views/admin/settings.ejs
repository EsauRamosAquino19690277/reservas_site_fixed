<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h3 class="mb-0">Ajustes del sitio</h3>
      <a class="btn btn-outline-secondary" href="/admin">Volver al panel</a>
    </div>

    <form action="/admin/ajustes" method="post" enctype="multipart/form-data" class="row g-3">
      <div class="col-md-6">
        <label class="form-label">Título del sitio</label>
        <input class="form-control" name="title" value="<%= s.title || '' %>">
      </div>

      <div class="col-md-3">
        <label class="form-label">Color navbar (HEX)</label>
        <input class="form-control" name="navbar_color" placeholder="#1b8f3a" value="<%= s.navbar_color || '' %>">
      </div>

      <div class="col-md-3">
        <label class="form-label">Teléfono</label>
        <input class="form-control" name="phone" value="<%= s.phone || '' %>">
      </div>

      <div class="col-md-4">
        <label class="form-label">Facebook URL</label>
        <input class="form-control" name="facebook_url" value="<%= s.facebook_url || '' %>">
      </div>

      <div class="col-md-4">
        <label class="form-label">Instagram URL</label>
        <input class="form-control" name="instagram_url" value="<%= s.instagram_url || '' %>">
      </div>

      <div class="col-md-4">
        <label class="form-label">TikTok URL</label>
        <input class="form-control" name="tiktok_url" value="<%= s.tiktok_url || '' %>">
      </div>

      <div class="col-12">
        <label class="form-label">Texto de privacidad (footer)</label>
        <textarea class="form-control" name="privacy_text" rows="2"><%= s.privacy_text || '' %></textarea>
      </div>

      <div class="col-md-6">
        <label class="form-label">Dirección</label>
        <input class="form-control" name="address" value="<%= s.address || '' %>">
      </div>

      <div class="col-md-6">
        <label class="form-label">Maps Embed URL</label>
        <input class="form-control" name="maps_embed_url"
               placeholder="https://www.google.com/maps/embed?pb=..."
               value="<%= s.maps_embed_url || '' %>">
        <div class="form-text">
          Pega el <strong>embed</strong> de Google Maps (debe iniciar con
          <code>https://www.google.com/maps/embed?pb=...</code>)
        </div>
      </div>

      <div class="col-md-6">
        <label class="form-label">Logo (subir nuevo)</label>
        <input type="file" class="form-control" name="logo_file" accept="image/*">
        <% if (s.logo_url) { %>
          <div class="form-text">Logo actual: <code><%= s.logo_url %></code></div>
          <img src="<%= s.logo_url %>" alt="Logo" style="height:48px" class="mt-2">
        <% } %>
      </div>
      <div class="card mt-4">
  <div class="card-header">
    Seguridad
  </div>
  <div class="card-body">
    <div class="mb-3">
      <label class="form-label">Nueva contraseña de administrador</label>
      <input
        type="password"
        name="new_admin_password"
        class="form-control"
        autocomplete="new-password"
      />
      <div class="form-text">
        Deja estos campos en blanco si no quieres cambiar la contraseña.
      </div>
    </div>

    <div class="mb-3">
      <label class="form-label">Confirmar nueva contraseña</label>
      <input
        type="password"
        name="confirm_admin_password"
        class="form-control"
        autocomplete="new-password"
      />
    </div>
  </div>
</div>


      <div class="col-12">
        <button class="btn btn-primary">Guardar</button>
      </div>
    </form>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
