<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %> · <%= site.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
    <style>:root{ --brand-green:<%= site.navbar_color || '#1b8f3a' %>; }</style>
  </head>
  <body>
    <main class="container my-5">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="alert alert-warning border shadow-sm">
            <h4 class="alert-heading">Archivo demasiado grande</h4>
            <p>El archivo que intentaste subir supera el tamaño máximo permitido.</p>
            <p class="mb-0"><strong>Límite:</strong> <%= maxMB %> MB.</p>
          </div>
          <a href="<%= backUrl %>" class="btn btn-outline-secondary">Volver</a>
        </div>
      </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
