<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %> · <%= site.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
<nav class="navbar navbar-expand-lg">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/admin/encuestas">
      <% if (site.logo_url) { %>
        <img src="<%= site.logo_url %>" class="brand-logo me-2" alt="Logo">
      <% } else { %>
        <strong><%= site.title %></strong>
      <% } %>
      <span class="badge bg-dark ms-2">Admin</span>
    </a>
  </div>
</nav>

<main class="container my-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h3 class="mb-0">Nueva encuesta</h3>
    <a href="/admin/encuestas" class="btn btn-outline-secondary btn-sm">Volver a encuestas</a>
  </div>

  <% if (error) { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>

  <form method="post" class="row g-3">
    <div class="col-12">
      <label class="form-label">Título</label>
      <input class="form-control" name="title" required
             value="<%= form.title || '' %>">
    </div>
    <div class="col-12">
      <label class="form-label">Descripción (opcional)</label>
      <textarea class="form-control" name="description" rows="2"><%= form.description || '' %></textarea>
    </div>
    <div class="col-12">
      <label class="form-label">Enlace externo (opcional)</label>
      <input class="form-control" name="external_url"
             placeholder="https://..." value="<%= form.external_url || '' %>">
      <div class="form-text">
        Si llenas este campo, la encuesta sólo mostrará un botón que redirige a este enlace
        y <strong>no se usarán las preguntas de abajo</strong>.
      </div>
    </div>

    <hr class="my-3">

    <div class="col-12">
      <p class="mb-1">
        Si NO usas enlace externo, define aquí las preguntas de la encuesta.
      </p>
      <p class="small text-muted mb-2">
        Mínimo 5 preguntas, máximo 15. Cada pregunta puede ser:
        <strong>opción múltiple</strong> (hasta 5 respuestas) o
        <strong>abierta</strong> (texto libre).
      </p>
    </div>

    <% for (let i = 1; i <= 15; i++) { %>
      <div class="col-12 border rounded p-3 mb-2">
        <h6 class="mb-2">Pregunta <%= i %></h6>
        <div class="row g-2">
          <div class="col-md-8">
            <label class="form-label">Texto de la pregunta</label>
            <input class="form-control" name="q_text_<%= i %>"
                   value="<%= form['q_text_' + i] || '' %>">
          </div>
          <div class="col-md-4">
            <label class="form-label">Tipo</label>
            <select class="form-select" name="q_type_<%= i %>">
              <option value="choice"
                <%= (form['q_type_' + i] || '') === 'open' ? '' : 'selected' %>>
                Opción múltiple
              </option>
              <option value="open"
                <%= (form['q_type_' + i] || '') === 'open' ? 'selected' : '' %>>
                Abierta (texto)
              </option>
            </select>
          </div>
        </div>
        <div class="row g-2 mt-2">
          <% for (let j = 1; j <= 5; j++) { %>
            <div class="col-md-6 col-lg-4">
              <label class="form-label">Opción <%= j %> (para preguntas de opción)</label>
              <input class="form-control" name="q_opt_<%= i %>_<%= j %>"
                     value="<%= form[`q_opt_${i}_${j}`] || '' %>">
            </div>
          <% } %>
        </div>
      </div>
    <% } %>

    <div class="col-12">
      <button class="btn btn-primary">Crear encuesta</button>
      <a href="/admin/encuestas" class="btn btn-outline-secondary ms-2">Cancelar</a>
    </div>
  </form>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
