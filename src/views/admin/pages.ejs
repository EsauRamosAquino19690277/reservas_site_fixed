<!doctype html><html lang="es"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title><%= title %> · <%= site.title %></title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="/css/styles.css" rel="stylesheet">
<style>:root{ --brand-green:<%= site.navbar_color || '#1b8f3a' %>; }</style>
</head><body>
<nav class="navbar navbar-expand-lg">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center gap-2" href="/">
      <% if(site.logo_url){ %><img src="<%= site.logo_url %>" class="brand-logo" alt="Logo"><% } else { %><strong><%= site.title %></strong><% } %>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav"><span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="nav">
    </div>
  </div>
</nav>
<main class="container my-4">
<div class="d-flex justify-content-between align-items-center mb-3">
  <h3 class="mb-0">Páginas</h3>
  <a href="/admin" class="btn btn-outline-secondary">Volver al panel</a>
</div>

<h5>Historia</h5>
<form method="post" action="/admin/paginas/historia" enctype="multipart/form-data" class="row g-3">
  <div class="col-md-6">
    <label class="form-label">Título</label>
    <input name="title" class="form-control"
           value="<%= historia?.title || 'Nuestra Historia' %>">
  </div>

  <div class="col-md-6">
    <label class="form-label">Subtítulo principal (opcional)</label>
    <input name="subtitle1" class="form-control"
           value="<%= historia?.subtitle1 || '' %>">
  </div>

  <div class="col-12">
    <label class="form-label">Contenido 1</label>
    <textarea name="body" class="form-control" rows="5"><%= historia?.body || '' %></textarea>
    <div class="form-text">
      Puedes usar saltos de renglón y espacios; se respetarán en la página pública.
    </div>
  </div>

  <hr class="mt-3">

  <div class="col-md-6">
    <label class="form-label">Subtítulo 2 (opcional)</label>
    <input name="subtitle2" class="form-control"
           value="<%= historia?.subtitle2 || '' %>">
  </div>

  <div class="col-12">
    <label class="form-label">Contenido 2 (opcional)</label>
    <textarea name="body2" class="form-control" rows="5"><%= historia?.body2 || '' %></textarea>
  </div>

  <div class="col-12">
    <label class="form-label">Imágenes/Videos</label>
    <input type="file" name="media_files" class="form-control" multiple>
    <small class="text-muted">Si no adjuntas, pega JSON abajo.</small>
  </div>

  <div class="col-12">
    <input name="media_json" class="form-control" placeholder="(Opcional) JSON de URLs">
  </div>

  <div class="col-12">
    <button class="btn btn-primary">Guardar</button>
  </div>
</form>

<% if(historia?.media_json){ %>
  <div class="mt-3 row g-2">
    <% JSON.parse(historia.media_json).forEach(function(src){ %>
      <div class="col-4"><img src="<%= src %>" class="img-fluid rounded"></div>
    <% }) %>
  </div>
<% } %>
<a href="/admin" class="btn btn-primary admin-back">Volver al panel</a>

</main>

<footer class="site-footer text-white mt-4" style="background:var(--brand-green);">

</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body></html>
